import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  typography: {
    fontFamily: 'Nunito Sans'
  },
  palette: {
    primary: {
      main: '#002e9f'
    },
    secondary: {
      main: '#0079ff'
    }
  }
})
